import TopBar from "../components/TopBar";
import MenuBar from "../components/MenuBar";

const HomePage = () => {
  return (
    <div>
      <TopBar />
      <MenuBar />
    </div>
  );
};

export default HomePage;
